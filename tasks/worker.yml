- name: "[worker] - kubeadm token create"
  shell: |
   sudo kubeadm join {{ groups['master'][0] }}:6443 --token {{ admission_token }} --discovery-token-unsafe-skip-ca-verification
  when: inventory_hostname in groups['worker']
  async: 300
  poll: 1
  ignore_errors: yes
  tags: joinworker